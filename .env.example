# =============================================================================
# NOMOS — Cristian Assistant
# Proyecto: cristian-assistant/ (Vite + React SPA)
#
# IMPORTANTE: El app frontend está en cristian-assistant/.
# Las variables VITE_* van en cristian-assistant/.env.local
# (ver cristian-assistant/.env.example para detalle).
# =============================================================================

# =============================================================================
# VERCEL — Variables de entorno para el deploy
# Configurar en: Vercel Dashboard > Project Settings > Environment Variables
# Root Directory en Vercel debe ser: cristian-assistant
# =============================================================================
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
# VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com  (opcional)

# =============================================================================
# SUPABASE EDGE FUNCTIONS — Secrets (NO van en Vercel)
# Configurar con: supabase secrets set KEY=value
# =============================================================================
# SUPABASE_SERVICE_ROLE_KEY   (auto-configurado por Supabase)
# OPENAI_API_KEY              (LangChain / chat / agente IA)
# RESEND_API_KEY              (emails transaccionales)
# FROM_EMAIL                  (ej: "Lisual <onboarding@resend.dev>")
# FATHOM_API_KEY              (Fathom AI — reuniones automáticas)
# FATHOM_WEBHOOK_SECRET       (verificación webhook Fathom)
# EVOLUTION_API_URL           (WhatsApp via Evolution API)
# EVOLUTION_API_KEY
# EVOLUTION_INSTANCE
# CRON_SECRET                 (protección del cron de seguimientos)
# ALLOWED_USER_EMAILS         (allowlist de correos, opcional)
