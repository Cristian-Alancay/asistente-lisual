# =============================================================================
# ASISTENTE LISUAL – Variables de entorno
# Copiá este archivo a .env.local y reemplazá cada COMPLETAR con tu valor real.
# Las que tienen valor de ejemplo (ej. tu-proyecto.supabase.co) también hay que reemplazarlas.
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase (obligatorio – login y datos)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=COMPLETAR
NEXT_PUBLIC_SUPABASE_ANON_KEY=COMPLETAR

# Supabase Service Role – para cron de seguimientos y herramientas del agente (leads, presupuestos).
# Sin esto: el agente no puede leer/escribir en la DB; el cron de seguimientos falla.
SUPABASE_SERVICE_ROLE_KEY=COMPLETAR

# -----------------------------------------------------------------------------
# Allowlist de usuarios (opcional)
# Si no definís nada, no se filtra por correo (útil en desarrollo).
# Lista de emails separados por coma. Ej: correo1@ejemplo.com,correo2@ejemplo.com
# -----------------------------------------------------------------------------
# ALLOWED_USER_EMAILS=COMPLETAR

# -----------------------------------------------------------------------------
# LangChain / OpenAI (obligatorio para agente, chat, detección de intención, OCR/visión)
# Sin esto: /api/chat y /api/agent devuelven 503; WhatsApp no usa IA.
# -----------------------------------------------------------------------------
OPENAI_API_KEY=COMPLETAR

# -----------------------------------------------------------------------------
# Evolution API – WhatsApp (webhook y envío de mensajes)
# Sin esto: no se pueden enviar respuestas por WhatsApp.
# -----------------------------------------------------------------------------
EVOLUTION_API_URL=COMPLETAR
EVOLUTION_API_KEY=COMPLETAR
EVOLUTION_INSTANCE=COMPLETAR

# -----------------------------------------------------------------------------
# Cron (Vercel Cron) – secreto para proteger la ruta de seguimientos
# Sin esto: el cron de seguimientos puede fallar o no estar protegido.
# -----------------------------------------------------------------------------
CRON_SECRET=COMPLETAR
